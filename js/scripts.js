// One language is never enough
// Oneway anguagelay isway evernay enoughway

// Business Logic
const stringInput = "One language is never enough";
function pigLatin(text) {
  text.toLowerCase();
  const vowel = ['a','e','i','o','u']
  console.log(text);
  const englishArray = text.split(" ");
  console.log(englishArray[0]); 
  word = englishArray[0];
  firstLetter = word.charAt(0);
  
  if (firstLetter) {
    console.log("there is a vowel");
  };
}
pigLatin(stringInput);



// UI Logic
